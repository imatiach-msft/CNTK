RootDir = .
DataDir = $RootDir$

# deviceId = -1 for CPU, >= 0 for GPU devices
deviceId = -1

frameMode = false
precision = "float"

Simple_Test = [
    action = "write"

    reader = [
        readerType = "DataFrameDeserializer"
        verbosity = 0

        file = "test.parquet"
    ]
]